import React from 'react';

const PortfolioContent = () => (
  <main className="portfolio">
    <p>This will be split rows linking to the other portfolio pages.</p>
  </main>
);

export default PortfolioContent;
